package com.optimalpay.recommendation.engine;

import org.springframework.stereotype.Component;

/**
 * Normalises heterogeneous reward types (cashback %, points per ₹, miles)
 * into a comparable monetary equivalent per unit spend.
 */
@Component
public class RewardNormaliser {
    // Implementation in Phase 2
}
