package com.optimalpay.recommendation.engine;

import org.springframework.stereotype.Component;

/**
 * Resolves spend context (merchant, category, amount) into a normalised
 * form that the recommendation engine can evaluate against reward rules.
 */
@Component
public class ContextResolver {
    // Implementation in Phase 2
}
